# aibe/monitoring/grafana/provisioning/dashboards.yml
apiVersion: 1

providers:
  - name: AIBE
    orgId: 1
    folder: AIBE
    folderUid: aibe
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards